[profile.default]
src = "src"
test = "test"
out = "out"
libs = []
solc_version = "0.8.24"
optimizer = true
optimizer_runs = 200

# Self-contained on purpose: no forge-std / OpenZeppelin submodules. The repo vendors
# everything it needs (see crates/nimmesh-core/src/evm_abi.rs — same no-framework rule);
# the tests declare the slice of the cheatcode surface they use in test/Cheats.sol.
