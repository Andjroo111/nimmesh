{
  "name": "nimmesh-fixtures",
  "private": true,
  "type": "module",
  "description": "Generates @nimiq/core reference fixtures the Rust G3 signer is asserted byte-exact against. Run: bun install && bun run gen",
  "scripts": {
    "gen": "bun run gen-fixtures.mjs"
  },
  "dependencies": {
    "@nimiq/core": "^2.5.0",
    "bitcoinjs-lib": "^7.0.1",
    "ecpair": "^3.0.1",
    "tiny-secp256k1": "^2.2.4"
  }
}
